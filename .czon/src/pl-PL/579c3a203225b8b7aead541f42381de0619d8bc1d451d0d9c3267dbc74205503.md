---
"title": "Test wyświetlania formuł KaTeX"
"summary": "Ten dokument jest stroną testową wyświetlania formuł KaTeX, prezentującą przykłady renderowania różnych formuł matematycznych. Dokument zawiera formuły w tekście, takie jak E=mc², formuły blokowe, takie jak całki i pochodne, a także złożone formuły sumowania, funkcje zdefiniowane przedziałami, macierze i tabele. Każda formuła jest opatrzona krótkim opisem, a celem jest sprawdzenie poprawności wyświetlania KaTeX w różnych scenariuszach. Dokument kończy się stwierdzeniem 'Proszę upewnić się, że wszystkie powyższe formuły są wyświetlane poprawnie', podkreślając jego cel testowy."
"tags":
  - "KaTeX"
  - "formuły matematyczne"
  - "test formuł"
  - "LaTeX"
  - "renderowanie matematyki"
  - "test dokumentu"
---

# Test wyświetlania formuł KaTeX

To jest test formuły w tekście: $ E = mc^2 $.

To jest test formuły blokowej:

$$
\int_a^b f(x) \, dx = F(b) - F(a)
$$

To jest kolejny test formuły blokowej:

$$
\frac{d}{dx}e^x = e^x
$$

To jest test złożonej formuły:

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

Test formuły z przypadkami:

$$
f(x) =
\begin{cases}
x^2 & \text{if } x \geq 0 \\
-x & \text{if } x < 0
\end{cases}
$$

Tabela w formule:

$$
\begin{array}{c|c|c}
a & b & c \\
\hline
1 & 2 & 3 \\
4 & 5 & 6 \\
\end{array}
$$

Macierz w formule:

$$
\begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$

Test formuły zawierającej tekst:

$$
E = mc^2 \quad \text{where } m \text{ is mass and
    } c \text{ is the speed of light}
$$

Uogólniony wzór Kelly'ego:

$$
f^* = \argmax_f \sum_{i \in X} P_i \ln (1 + f \cdot R_i)
$$

Proszę upewnić się, że wszystkie powyższe formuły są wyświetlane poprawnie.