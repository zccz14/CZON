---
'title': 'CZON - Trình tạo trang tĩnh Markdown + AI theo phong cách tối giản'
'summary': 'CZON là trình tạo trang tĩnh tích hợp AI, được thiết kế riêng cho tài liệu Markdown. Nó cho phép người dùng nhanh chóng biến một thư mục Git chứa file Markdown thành trang web HTML tĩnh mà không cần cấu hình phức tạp. Tính năng cốt lõi bao gồm điều hướng phân loại thông minh, tự động tạo sơ đồ trang và điều hướng thông qua phân tích nội dung bằng AI, cùng với dịch thuật tăng dần tự động, hỗ trợ người dùng viết bằng ngôn ngữ mẹ đẻ và tạo phiên bản đa ngôn ngữ chỉ với một cú nhấp chuột. Công cụ nhấn mạnh việc trở về bản chất của nội dung, tối thiểu hóa sự xáo trộn khi viết, và sử dụng LLM để xử lý các tác vụ như tạo tiêu đề, tóm tắt, dịch thuật, đơn giản hóa quy trình quản lý tài liệu. Phần bắt đầu nhanh cung cấp hướng dẫn chi tiết về cấu hình môi trường và lệnh xây dựng.'
'tags':
  - 'Trình tạo trang tĩnh'
  - 'Markdown'
  - 'AI'
  - 'Dịch đa ngôn ngữ'
  - 'Chủ nghĩa tối giản'
  - 'Công cụ tài liệu'
  - 'Git'
  - 'Node.js'
'inferred_lang': 'zh-Hans'
---

# CZON - Trình tạo trang tĩnh Markdown + AI theo phong cách tối giản

[> Xem Demo Trang Web](https://czon.zccz14.com/)

## Chức năng cơ bản

1. 📃 **Tạo trang tĩnh**: Biến bất kỳ thư mục git nào chứa Markdown thành một trang web HTML tĩnh.
2. 🧭 **Điều hướng phân loại thông minh**: Sử dụng AI để phân tích nội dung, tạo sơ đồ trang và điều hướng, không phụ thuộc vào vị trí file nguồn.
3. 🌍 **Dịch thuật tăng dần tự động**: Sử dụng AI để dịch tăng dần, cho phép người dùng viết Markdown bằng ngôn ngữ mẹ đẻ nhưng vẫn có thể tạo phiên bản đa ngôn ngữ.

Tại sao lại tạo thêm một trình tạo trang tĩnh Markdown nữa? Tôi đã suy nghĩ lại về cách công cụ tài liệu có thể phục vụ người viết tốt hơn trong thời đại AI:

- Trở về với nội dung: Tối thiểu hóa sự xáo trộn, tập trung vào việc viết

  Đưa tài liệu trở về bản chất, đưa việc viết trở về sự tĩnh lặng.

  Thứ nhất, tôi không muốn làm quản thư. Đôi khi tôi muốn viết một số nội dung, nhưng có thể tôi chưa nghĩ ra tiêu đề cho nó, chưa hình thành cấu trúc tổ chức cụ thể. Tôi hy vọng LLM sẽ tự động giúp tôi tạo tiêu đề, tóm tắt, phân loại, điều hướng và các công việc sắp xếp khác.

  Thứ hai, thư phòng của tôi phải được dọn dẹp sạch sẽ. Tôi không muốn cấu hình các công cụ xây dựng phức tạp, mất công với cấu hình tài liệu phức tạp, không thích cấu trúc rườm rà. Mỗi lần nhìn thấy file cấu hình và cấu trúc dự án của các công cụ như Docusaurus, VuePress, Astro, tôi lại đau đầu. Giờ đây, CZON chọn cách ẩn tất cả cấu hình vào thư mục `.czon`, dẹp vào một góc, người dùng chỉ cần tập trung vào việc viết nội dung, phần còn lại để CZON xử lý. Các thư mục khác đều là không gian viết của bạn, bạn có thể tổ chức file Markdown tùy ý.

- Trở về với tiếng mẹ đẻ: Viết bằng tiếng mẹ đẻ, xây dựng phiên bản đa ngôn ngữ mượt mà

  Đời người ngắn ngủi, tôi lười dịch thuật. Con người chỉ có thể phát huy tối đa chiều sâu tư duy khi sử dụng ngôn ngữ mẹ đẻ. Nhưng đồng thời, việc kết nối với thế giới là cần thiết, tôi không muốn từ bỏ giao tiếp đa ngôn ngữ. Vì vậy, tôi hy vọng có một công cụ cho phép tôi viết bằng tiếng mẹ đẻ, đồng thời lại có thể để nhiều người hơn xem được nội dung của tôi. Hiện tại, i18n không chỉ yêu cầu tự dịch mà còn phải tự duy trì cập nhật nội dung, quá phiền phức. Tôi chọn sử dụng LLM để giúp tôi dịch thuật, tiết kiệm rất nhiều thời gian và có thể hoàn thành dịch sang nhiều ngôn ngữ chỉ với một cú nhấp chuột.

## Bắt đầu nhanh

Điều kiện tiên quyết:

- Đã cài đặt [Node.js](https://nodejs.org/) (khuyến nghị phiên bản 18 trở lên, cần có lệnh npx)
- Đã có [OpenAI API Key](https://platform.openai.com/account/api-keys), hoặc API Key tương thích với OPENAI
- Đã cài đặt Git (dùng để liệt kê file Markdown từ Git, bỏ qua các file bị lọc bởi quy tắc .gitignore)

Đầu tiên, làm việc trong một thư mục đã được quản lý bởi git:

Cấu hình biến môi trường

```bash
export OPENAI_API_KEY="sk-xxxxxx"  # Bắt buộc: Vui lòng thay thế bằng OpenAI API Key của bạn
export OPENAI_BASE_URL="https://api.openai.com/v1"  # Tùy chọn, mặc định sử dụng API chính thức của OpenAI
export OPENAI_MODEL="gpt-3.5-turbo"  # Tùy chọn, mặc định sử dụng mô hình gpt-3.5-turbo
```

Xây dựng trang web, hỗ trợ dịch đa ngôn ngữ. Sử dụng thư mục hiện tại làm thư mục nguồn, xuất ra thư mục `.czon/dist`.

- Có thể chỉ định phiên bản ngôn ngữ cần tạo bằng cách sử dụng nhiều lần tham số `--lang` (ví dụ: `--lang zh-Hans --lang en-US`).
- Không cần cấu hình ngôn ngữ nguồn, CZON sẽ tự động phát hiện.

```bash
npx czon@latest build --lang zh-Hans --lang en-US
```

**Xem thêm tham số hoặc trợ giúp**:

```bash
npx czon@latest
```
