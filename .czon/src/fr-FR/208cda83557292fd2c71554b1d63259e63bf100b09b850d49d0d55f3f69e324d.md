---
'title': 'CZON - Générateur de site statique minimaliste Markdown + IA'
'summary': "CZON est un générateur de site statique intégrant des fonctionnalités d'IA, conçu spécifiquement pour les documents Markdown. Il permet aux utilisateurs de transformer rapidement un dossier Git contenant des fichiers Markdown en un site HTML statique, sans configuration complexe. Les fonctionnalités principales incluent une navigation par catégorisation intelligente, qui génère automatiquement un plan du site et une navigation via l'analyse du contenu par IA, ainsi qu'une traduction incrémentielle automatique, permettant aux utilisateurs d'écrire dans leur langue maternelle et de générer en un clic des versions multilingues. L'outil met l'accent sur un retour à l'essence du contenu, en minimisant les distractions à l'écriture, et utilise des LLM pour gérer des tâches telles que la génération de titres, les résumés, la traduction, simplifiant ainsi le processus de gestion documentaire. La section Démarrage rapide fournit des instructions détaillées pour la configuration de l'environnement et les commandes de construction."
'tags':
  - 'Générateur de site statique'
  - 'Markdown'
  - 'IA'
  - 'Traduction multilingue'
  - 'Minimalisme'
  - 'Outil de documentation'
  - 'Git'
  - 'Node.js'
'inferred_lang': 'zh-Hans'
---

# CZON - Générateur de site statique minimaliste Markdown + IA

[> Démo du site web](https://czon.zccz14.com/)

## Fonctionnalités de base

1. 📃 **Génération de site statique** : Transforme n'importe quel dossier Git contenant des fichiers Markdown en un site HTML statique.
2. 🧭 **Navigation par catégorisation intelligente** : Utilise l'IA pour analyser le contenu, générer un plan du site et une navigation, indépendamment de l'emplacement des fichiers sources.
3. 🌍 **Traduction incrémentielle automatique** : Utilise l'IA pour une traduction incrémentielle, permettant aux utilisateurs d'écrire en Markdown dans leur langue maternelle, tout en offrant un contenu multilingue.

Pourquoi créer un autre générateur de site statique Markdown ? J'ai repensé la manière dont les outils de documentation pourraient mieux servir les rédacteurs à l'ère de l'IA :

- Retour au contenu : Minimiser les distractions, se concentrer sur l'écriture

  Ramener la documentation à son essence, ramener la sérénité à l'écriture.

  Premièrement, je ne veux pas être un bibliothécaire. Parfois, je veux écrire du contenu, mais je n'ai peut-être pas encore trouvé de titre, ni établi d'organisation particulière. Je souhaite que les LLM génèrent automatiquement des titres, fassent des résumés, classent, organisent la navigation, etc.

  Deuxièmement, mon espace de travail doit être rangé. Je ne veux pas d'outils de construction complexes à configurer, ni de configurations documentaires compliquées, ni de structures alambiquées. Chaque fois que je vois les fichiers de configuration et les structures de projet d'outils comme Docusaurus, VuePress, Astro, j'ai mal à la tête. Désormais, CZON choisit de cacher toute la configuration dans le répertoire `.czon`, de la reléguer dans un coin, afin que l'utilisateur se concentre uniquement sur le contenu de l'écriture, et laisse CZON gérer le reste. Tous les autres répertoires sont votre espace d'écriture, vous pouvez organiser vos fichiers Markdown comme bon vous semble.

- Retour à la langue maternelle : Écrire dans sa langue maternelle, construire des versions multilingues de manière fluide

  La vie est courte, je n'ai pas envie de traduire. C'est dans sa langue maternelle qu'une personne peut exprimer la profondeur de sa réflexion. Mais il est aussi nécessaire de rester connecté au monde, et je ne veux pas renoncer à la communication multilingue. J'espère donc avoir un outil qui me permette d'écrire dans ma langue maternelle, tout en permettant à plus de personnes de voir mon contenu. Actuellement, l'i18n nécessite non seulement de traduire soi-même, mais aussi de maintenir les mises à jour du contenu, ce qui est trop fastidieux. J'ai choisi d'utiliser les LLM pour m'aider à traduire, ce qui me fait gagner beaucoup de temps et permet de générer des traductions en plusieurs langues en un clic.

## Démarrage rapide

Prérequis :

- [Node.js](https://nodejs.org/) installé (version 18 ou supérieure recommandée, nécessite la commande `npx`)
- Clé API [OpenAI](https://platform.openai.com/account/api-keys) obtenue, ou une clé API compatible avec OPENAI
- Git installé (pour lister les fichiers Markdown à partir de Git, en ignorant les fichiers filtrés par les règles `.gitignore`)

Tout d'abord, travaillez dans un dossier déjà géré par Git :

Configurer les variables d'environnement

```bash
export OPENAI_API_KEY="sk-xxxxxx"  # Obligatoire : remplacez par votre clé API OpenAI
export OPENAI_BASE_URL="https://api.openai.com/v1"  # Optionnel, utilise par défaut l'API officielle d'OpenAI
export OPENAI_MODEL="gpt-3.5-turbo"  # Optionnel, utilise par défaut le modèle gpt-3.5-turbo
```

Construire le site, avec prise en charge de la traduction multilingue. Utilise le répertoire courant comme répertoire source, et sort les fichiers dans le répertoire `.czon/dist`.

- Il est possible de spécifier les versions linguistiques à générer en utilisant plusieurs fois le paramètre `--lang` (par exemple `--lang zh-Hans --lang en-US`).
- Aucune configuration de la langue source n'est nécessaire, CZON la détecte automatiquement.

```bash
npx czon@latest build --lang zh-Hans --lang en-US
```

**Voir plus de paramètres ou obtenir de l'aide** :

```bash
npx czon@latest
```
