---
'title': 'CZON - Минималистичный Markdown + AI генератор статических сайтов'
'summary': 'CZON — это генератор статических сайтов с функциями искусственного интеллекта, разработанный специально для документов Markdown. Он позволяет пользователям быстро преобразовывать Git-папки с файлами Markdown в статические HTML-сайты без сложной настройки. Ключевые функции включают интеллектуальную навигацию по категориям, где AI анализирует содержимое для автоматического создания карты сайта и навигации, а также автоматический инкрементный перевод, позволяющий пользователям писать на родном языке и одним кликом создавать многоязычные версии. Инструмент подчеркивает возвращение к сути контента, минимизируя отвлекающие факторы при написании, и использует LLM для обработки таких задач, как генерация заголовков, создание резюме, перевод и упрощение процесса управления документами. В разделе «Быстрый старт» приведены подробные инструкции по настройке среды и командам сборки.'
'tags':
  - 'Генератор статических сайтов'
  - 'Markdown'
  - 'Искусственный интеллект'
  - 'Многоязычный перевод'
  - 'Минимализм'
  - 'Инструменты для документов'
  - 'Git'
  - 'Node.js'
'inferred_lang': 'zh-Hans'
---

# CZON - Минималистичный Markdown + AI генератор статических сайтов

[> Демо сайта](https://czon.zccz14.com/)

## Основные функции

1. 📃 **Генерация статических сайтов**: Преобразует любую Git-папку с файлами Markdown в статический HTML-сайт.
2. 🧭 **Интеллектуальная навигация по категориям**: Использует AI для анализа содержимого, создания карты сайта и навигации, независимо от расположения исходных файлов.
3. 🌍 **Автоматический инкрементный перевод**: Использует AI для инкрементного перевода, позволяя пользователям писать Markdown на родном языке, но при этом создавать многоязычный контент.

Зачем ещё один генератор статических сайтов на Markdown? Я задумался о том, как инструменты для документов в эпоху AI могут лучше служить авторам:

- Возвращение к контенту: Минимизация отвлекающих факторов, концентрация на написании

  Вернуть документам их суть, а написанию — спокойствие.

  Во-первых, я не хочу быть библиотекарем. Иногда я хочу записать что-то, но ещё не придумал заголовок или не организовал материал. Я хочу, чтобы LLM автоматически генерировала заголовки, создавала резюме, классифицировала, организовывала навигацию и другие задачи по упорядочиванию.

  Во-вторых, мой рабочий кабинет должен быть чистым. Мне не нужны сложные инструменты сборки с запутанной конфигурацией, сложные настройки документов или громоздкие структуры. Каждый раз, когда я вижу конфигурационные файлы и структуры проектов таких инструментов, как Docusaurus, VuePress или Astro, у меня начинает болеть голова. Теперь CZON скрывает все настройки в каталоге `.czon`, убирая их с глаз долой. Пользователю нужно сосредоточиться только на содержании, а всё остальное CZON берёт на себя. Все остальные каталоги — это ваше пространство для написания, где вы можете свободно организовывать свои файлы Markdown.

- Возвращение к родному языку: Пишите на родном языке, плавно создавайте многоязычные версии

  Жизнь коротка, мне лень переводить. Человек наиболее полно раскрывает глубину своего мышления, используя родной язык. Но при этом оставаться на связи с миром необходимо, и я не хочу отказываться от многоязычного общения. Поэтому мне нужен инструмент, который позволит мне писать на родном языке, но при этом сделать мой контент доступным для большего числа людей. Текущие решения i18n требуют не только самостоятельного перевода, но и поддержки обновлений контента, что слишком хлопотно. Я выбираю использование LLM для перевода, что экономит много времени и позволяет одним кликом создавать версии на нескольких языках.

## Быстрый старт

Предварительные требования:

- Установлен [Node.js](https://nodejs.org/) (рекомендуется версия 18 и выше, должна быть доступна команда `npx`)
- Получен [OpenAI API Key](https://platform.openai.com/account/api-keys) или совместимый с OPENAI API Key
- Установлен Git (для получения списка файлов Markdown из Git, игнорируя файлы, отфильтрованные по правилам `.gitignore`)

Сначала работайте в папке, которая уже управляется Git:

Настройте переменные окружения

```bash
export OPENAI_API_KEY="sk-xxxxxx"  # Обязательно: замените на ваш OpenAI API Key
export OPENAI_BASE_URL="https://api.openai.com/v1"  # Опционально, по умолчанию используется официальный API OpenAI
export OPENAI_MODEL="gpt-3.5-turbo"  # Опционально, по умолчанию используется модель gpt-3.5-turbo
```

Соберите сайт с поддержкой многоязычного перевода. Используйте текущий каталог как исходный, вывод в каталог `.czon/dist`.

- Можно несколько раз использовать параметр `--lang` для указания нужных языковых версий (например, `--lang zh-Hans --lang en-US`).
- Исходный язык настраивать не нужно, CZON определит его автоматически.

```bash
npx czon@latest build --lang zh-Hans --lang en-US
```

**Просмотреть больше параметров или справку**:

```bash
npx czon@latest
```
